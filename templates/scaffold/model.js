const mongoose = require("mongoose");
const Schema = mongoose.Schema;

// Creates the schema for the database object
const [MODEL_NAME] = new Schema([MODEL_OBJECT]);

module.exports = mongoose.model([MONGOOSE_ARGS]);
