module.exports = {
  // general errors
  dir_exists: "A directory with that name already exists.",
  file_exists: "A file with that name already exists.",
  columns_exists_on_table:
    "Attempting to add a column that already exists on that table.",
  model_does_not_exist: "That model does not yet exist: ",

  // create cmd errors
  omit_no_match:
    "The option you passed in the omit flag does not match 'client' or 'server'."
};
